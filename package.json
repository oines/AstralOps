{
  "name": "astralops",
  "version": "0.3.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "workspaces": [
    "apps/desktop",
    "protocol"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "build": "npm run build -w apps/desktop && npm run build -w protocol && go build ./daemon ./proxy-agent ./relay",
    "test": "go test ./...",
    "check": "npm run check -w apps/desktop && npm run check -w protocol && go test ./...",
    "package:desktop": "node scripts/package-desktop.mjs"
  },
  "devDependencies": {
    "electron-builder": "^26.8.1"
  }
}
