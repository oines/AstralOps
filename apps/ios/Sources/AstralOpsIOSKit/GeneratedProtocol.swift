// Code generated by tools/protocolgen. DO NOT EDIT.

import Foundation

enum GeneratedProtocol {
  struct ActionError: Codable, Equatable {
    var status: Int
    var code: ControlErrorCode
    var message: String
    var details: [String: String]

    enum CodingKeys: String, CodingKey {
      case status
      case code
      case message
      case details
    }
  }

  struct AgentInfo: Codable, Equatable {
    var path: String?
    var version: String?
    var available: Bool
    var currentModel: String?
    var currentEffort: String?
    var models: [ModelInfo]?

    enum CodingKeys: String, CodingKey {
      case path
      case version
      case available
      case currentModel = "current_model"
      case currentEffort = "current_effort"
      case models
    }
  }

  enum AgentKind: String, Codable, Equatable, Sendable {
    case agentClaude = "claude"
    case agentCodex = "codex"
  }

  struct ApprovalRequestedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var additionalPermissions: JSONValue? { storage["additional_permissions"] }

    var approvalId: JSONValue? { storage["approval_id"] }

    var availableDecisions: JSONValue? { storage["available_decisions"] }

    var changes: JSONValue? { storage["changes"] }

    var command: JSONValue? { storage["command"] }

    var commandActions: JSONValue? { storage["command_actions"] }

    var cwd: JSONValue? { storage["cwd"] }

    var decision: JSONValue? { storage["decision"] }

    var diff: JSONValue? { storage["diff"] }

    var itemId: JSONValue? { storage["item_id"] }

    var kind: JSONValue? { storage["kind"] }

    var networkApprovalContext: JSONValue? { storage["network_approval_context"] }

    var options: JSONValue? { storage["options"] }

    var params: JSONValue? { storage["params"] }

    var path: JSONValue? { storage["path"] }

    var plan: JSONValue? { storage["plan"] }

    var proposedExecpolicyAmendment: JSONValue? { storage["proposed_execpolicy_amendment"] }

    var proposedNetworkAmendments: JSONValue? { storage["proposed_network_amendments"] }

    var question: JSONValue? { storage["question"] }

    var reason: JSONValue? { storage["reason"] }

    var requestId: JSONValue? { storage["request_id"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }

    var toolInput: JSONValue? { storage["tool_input"] }

    var toolName: JSONValue? { storage["tool_name"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct ApprovalResolvedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var approvalId: JSONValue? { storage["approval_id"] }

    var decision: JSONValue? { storage["decision"] }

    var requestId: JSONValue? { storage["request_id"] }

    var response: JSONValue? { storage["response"] }

    var source: JSONValue? { storage["source"] }
  }

  struct ApprovalRespondedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var approvalId: JSONValue? { storage["approval_id"] }

    var decision: JSONValue? { storage["decision"] }

    var requestId: JSONValue? { storage["request_id"] }

    var response: JSONValue? { storage["response"] }

    var source: JSONValue? { storage["source"] }
  }

  struct AskRequestedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var allowCustom: JSONValue? { storage["allowCustom"] ?? storage["allow_custom"] }

    var askId: JSONValue? { storage["ask_id"] }

    var fields: JSONValue? { storage["fields"] }

    var isSecret: JSONValue? { storage["isSecret"] ?? storage["is_secret"] }

    var itemId: JSONValue? { storage["item_id"] }

    var kind: JSONValue? { storage["kind"] }

    var mode: JSONValue? { storage["mode"] }

    var multiSelect: JSONValue? { storage["multiSelect"] ?? storage["multi_select"] }

    var options: JSONValue? { storage["options"] }

    var params: JSONValue? { storage["params"] }

    var prompt: JSONValue? { storage["prompt"] }

    var question: JSONValue? { storage["question"] }

    var requestId: JSONValue? { storage["request_id"] }

    var requestedSchema: JSONValue? { storage["requestedSchema"] ?? storage["requested_schema"] }

    var schema: JSONValue? { storage["schema"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }

    var turnId: JSONValue? { storage["turn_id"] }

    var url: JSONValue? { storage["url"] }
  }

  struct AskResolvedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var answers: JSONValue? { storage["answers"] }

    var askId: JSONValue? { storage["ask_id"] }

    var requestId: JSONValue? { storage["request_id"] }

    var response: JSONValue? { storage["response"] }

    var source: JSONValue? { storage["source"] }
  }

  struct AstralEvent: Codable, Equatable {
    var seq: Int64
    var ts: String
    var workspaceId: String
    var sessionId: String
    var agent: AgentKind
    var kind: AstralEventKind
    var normalized: AstralNormalizedPayload
    var raw: JSONValue?

    enum CodingKeys: String, CodingKey {
      case seq
      case ts
      case workspaceId = "workspace_id"
      case sessionId = "session_id"
      case agent
      case kind
      case normalized
      case raw
    }

    init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      seq = try container.decode(Int64.self, forKey: .seq)
      ts = try container.decode(String.self, forKey: .ts)
      workspaceId = try container.decode(String.self, forKey: .workspaceId)
      sessionId = try container.decode(String.self, forKey: .sessionId)
      agent = try container.decode(AgentKind.self, forKey: .agent)
      kind = try container.decode(AstralEventKind.self, forKey: .kind)
      normalized = try AstralNormalizedPayload.decode(kind: kind, from: container, forKey: .normalized)
      raw = try container.decodeIfPresent(JSONValue.self, forKey: .raw)
    }

    func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(seq, forKey: .seq)
      try container.encode(ts, forKey: .ts)
      try container.encode(workspaceId, forKey: .workspaceId)
      try container.encode(sessionId, forKey: .sessionId)
      try container.encode(agent, forKey: .agent)
      try container.encode(kind, forKey: .kind)
      try container.encode(normalized, forKey: .normalized)
      try container.encodeIfPresent(raw, forKey: .raw)
    }
  }

  enum AstralEventFamily: String, Codable, Equatable, Sendable {
    case approval = "approval"
    case ask = "ask"
    case control = "control"
    case hook = "hook"
    case memory = "memory"
    case message = "message"
    case plan = "plan"
    case queue = "queue"
    case reasoning = "reasoning"
    case session = "session"
    case subagent = "subagent"
    case tool = "tool"
    case turn = "turn"
    case workspace = "workspace"
  }

  enum AstralEventKind: String, Codable, Equatable, Sendable {
    case approvalRequested = "approval.requested"
    case approvalResolved = "approval.resolved"
    case approvalResponded = "approval.responded"
    case askRequested = "ask.requested"
    case askResolved = "ask.resolved"
    case controlContext = "control.context"
    case controlError = "control.error"
    case controlInterrupt = "control.interrupt"
    case controlModel = "control.model"
    case controlNotification = "control.notification"
    case controlPairingApproved = "control.pairing.approved"
    case controlPairingDenied = "control.pairing.denied"
    case controlPairingRequested = "control.pairing.requested"
    case controlRateLimit = "control.rate_limit"
    case controlRaw = "control.raw"
    case controlStatus = "control.status"
    case controlSteer = "control.steer"
    case controlTerminalAttached = "control.terminal.attached"
    case controlTerminalClosed = "control.terminal.closed"
    case controlTerminalDetached = "control.terminal.detached"
    case controlTerminalOpened = "control.terminal.opened"
    case controlTrustGranted = "control.trust.granted"
    case controlTrustRevoked = "control.trust.revoked"
    case controlWarning = "control.warning"
    case hookCompleted = "hook.completed"
    case hookProgress = "hook.progress"
    case hookStarted = "hook.started"
    case memoryCompacted = "memory.compacted"
    case memoryCompacting = "memory.compacting"
    case messageAssistant = "message.assistant"
    case messageDelta = "message.delta"
    case messageMedia = "message.media"
    case messageStarted = "message.started"
    case messageUser = "message.user"
    case planDelta = "plan.delta"
    case planUpdated = "plan.updated"
    case queueCancelled = "queue.cancelled"
    case queueDequeued = "queue.dequeued"
    case queueFailed = "queue.failed"
    case queueQueued = "queue.queued"
    case queueSteered = "queue.steered"
    case reasoningCompleted = "reasoning.completed"
    case reasoningDelta = "reasoning.delta"
    case reasoningStarted = "reasoning.started"
    case sessionDeleted = "session.deleted"
    case sessionNative = "session.native"
    case sessionStarted = "session.started"
    case sessionUpdated = "session.updated"
    case toolCompleted = "tool.completed"
    case toolDiff = "tool.diff"
    case toolOutputDelta = "tool.output_delta"
    case toolProgress = "tool.progress"
    case toolStarted = "tool.started"
    case toolTodo = "tool.todo"
    case turnCancelled = "turn.cancelled"
    case turnCompleted = "turn.completed"
    case turnFailed = "turn.failed"
    case turnReplaced = "turn.replaced"
    case turnStarted = "turn.started"
    case workspaceConnection = "workspace.connection"
    case workspaceCreated = "workspace.created"
    case workspaceRemoved = "workspace.removed"
  }

  struct AstralEventNormalizedFieldSpec: Codable, Equatable {
    var name: String

    enum CodingKeys: String, CodingKey {
      case name
    }
  }

  struct AttachmentIngestChunkParams: Codable, Equatable {
    var sessionId: String
    var uploadId: String
    var seq: Int64
    var offset: Int64
    var dataBase64: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case uploadId = "upload_id"
      case seq
      case offset
      case dataBase64 = "data_base64"
    }
  }

  struct AttachmentIngestChunkResult: Codable, Equatable {
    var sessionId: String
    var uploadId: String
    var seq: Int64
    var offset: Int64
    var receivedBytes: Int64

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case uploadId = "upload_id"
      case seq
      case offset
      case receivedBytes = "received_bytes"
    }
  }

  struct AttachmentIngestFinishParams: Codable, Equatable {
    var sessionId: String
    var uploadId: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case uploadId = "upload_id"
    }
  }

  struct AttachmentIngestFinishResult: Codable, Equatable {
    var sessionId: String
    var uploadId: String
    var attachment: ControlAttachmentHandle

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case uploadId = "upload_id"
      case attachment
    }
  }

  struct AttachmentIngestParams: Codable, Equatable {
    var sessionId: String
    var name: String
    var kind: String
    var mimeType: String
    var detail: String
    var contentBase64: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case name
      case kind
      case mimeType = "mime_type"
      case detail
      case contentBase64 = "content_base64"
    }
  }

  struct AttachmentIngestResult: Codable, Equatable {
    var sessionId: String
    var attachment: ControlAttachmentHandle

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case attachment
    }
  }

  struct AttachmentIngestStartParams: Codable, Equatable {
    var sessionId: String
    var name: String
    var kind: String
    var mimeType: String
    var detail: String
    var size: Int64?
    var sha256: String?

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case name
      case kind
      case mimeType = "mime_type"
      case detail
      case size
      case sha256
    }
  }

  struct AttachmentIngestStartResult: Codable, Equatable {
    var sessionId: String
    var uploadId: String
    var attachmentId: String
    var chunkMaxBytes: Int64
    var maxBytes: Int64

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case uploadId = "upload_id"
      case attachmentId = "attachment_id"
      case chunkMaxBytes = "chunk_max_bytes"
      case maxBytes = "max_bytes"
    }
  }

  enum ControlAction: String, Codable, Equatable, Sendable {
    case attachmentIngest = "attachment.ingest"
    case attachmentIngestChunk = "attachment.ingest.chunk"
    case attachmentIngestFinish = "attachment.ingest.finish"
    case attachmentIngestStart = "attachment.ingest.start"
    case interrupt = "core.control.interrupt"
    case nativeSessionImport = "core.control.native_session.import"
    case queueCancel = "core.control.queue.cancel"
    case queueSteer = "core.control.queue.steer"
    case sessionCreate = "core.control.session.create"
    case sessionDelete = "core.control.session.delete"
    case sessionFork = "core.control.session.fork"
    case sessionInput = "core.control.session_input"
    case workspaceConnect = "core.control.workspace.connect"
    case workspaceCreate = "core.control.workspace.create"
    case workspaceDelete = "core.control.workspace.delete"
    case workspaceDisconnect = "core.control.workspace.disconnect"
    case events = "core.read.events"
    case hostSnapshot = "core.read.host_snapshot"
    case nativeSessions = "core.read.native_sessions"
    case ping = "core.read.ping"
    case sessionView = "core.read.session_view"
    case sessions = "core.read.sessions"
    case workbench = "core.read.workbench"
    case workspaceConnection = "core.read.workspace.connection"
    case workspaces = "core.read.workspaces"
    case eventsSubscribe = "core.subscribe.events"
    case eventsUnsubscribe = "core.unsubscribe.events"
    case hostFileSystemBrowse = "host.fs.browse"
    case hostPairingApprove = "host.pairing.approve"
    case hostPairingDeny = "host.pairing.deny"
    case hostPairingList = "host.pairing.list"
    case hostTrustList = "host.trust.list"
    case hostTrustRevoke = "host.trust.revoke"
    case interactionRespond = "interaction.respond"
    case mediaDownload = "media.download"
    case mediaRead = "media.read"
    case mediaStream = "media.stream"
    case mediaStreamCancel = "media.stream.cancel"
    case sessionEdit = "session.edit"
    case terminalAttach = "terminal.attach"
    case terminalClose = "terminal.close"
    case terminalDetach = "terminal.detach"
    case terminalHeartbeatAck = "terminal.heartbeat_ack"
    case terminalInput = "terminal.input"
    case terminalList = "terminal.list"
    case terminalOpen = "terminal.open"
    case terminalResize = "terminal.resize"
    case workspaceExec = "workspace.exec"
    case workspaceFilesApplyPatch = "workspace.files.apply_patch"
    case workspaceFilesDelete = "workspace.files.delete"
    case workspaceFilesMove = "workspace.files.move"
    case workspaceFilesRead = "workspace.files.read"
    case workspaceFilesStream = "workspace.files.stream"
    case workspaceFilesStreamCancel = "workspace.files.stream.cancel"
    case workspaceFilesWrite = "workspace.files.write"
  }

  struct ControlActionSpec: Codable, Equatable {
    var action: ControlAction
    var capability: ControlCapability
    var paramsType: String?
    var resultType: String?

    enum CodingKeys: String, CodingKey {
      case action
      case capability
      case paramsType = "params_type"
      case resultType = "result_type"
    }
  }

  struct ControlAttachmentHandle: Codable, Equatable {
    var id: String
    var mediaId: String
    var kind: String
    var name: String
    var mimeType: String?
    var size: Int64?
    var detail: String?
    var hostOwned: Bool

    enum CodingKeys: String, CodingKey {
      case id
      case mediaId = "media_id"
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case detail
      case hostOwned = "host_owned"
    }
  }

  enum ControlCapability: String, Codable, Equatable, Sendable {
    case capabilityAttachmentIngest = "attachment.ingest"
    case capabilityCoreControl = "core.control"
    case capabilityCoreRead = "core.read"
    case capabilityHostFileSystemBrowse = "host.fs.browse"
    case capabilityHostManage = "host.manage"
    case capabilityInteractionRespond = "interaction.respond"
    case capabilityMediaDownload = "media.download"
    case capabilityMediaRead = "media.read"
    case capabilityMediaStream = "media.stream"
    case capabilitySessionEdit = "session.edit"
    case capabilityTerminalInput = "terminal.input"
    case capabilityTerminalOpen = "terminal.open"
    case capabilityWorkspaceExec = "workspace.exec"
    case capabilityWorkspaceFilesRead = "workspace.files.read"
    case capabilityWorkspaceFilesWrite = "workspace.files.write"
  }

  struct ControlContextNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var cacheCreationInputTokens: JSONValue? { storage["cache_creation_input_tokens"] }

    var cachedInputTokens: JSONValue? { storage["cached_input_tokens"] }

    var cumulativeCacheCreationInputTokens: JSONValue? { storage["cumulative_cache_creation_input_tokens"] }

    var cumulativeCachedInputTokens: JSONValue? { storage["cumulative_cached_input_tokens"] }

    var cumulativeInputTokens: JSONValue? { storage["cumulative_input_tokens"] }

    var cumulativeOutputTokens: JSONValue? { storage["cumulative_output_tokens"] }

    var cumulativeTotalTokens: JSONValue? { storage["cumulative_total_tokens"] }

    var inputTokens: JSONValue? { storage["input_tokens"] }

    var model: JSONValue? { storage["model"] }

    var modelContextWindow: JSONValue? { storage["model_context_window"] }

    var modelUsage: JSONValue? { storage["model_usage"] }

    var nativeSessionId: JSONValue? { storage["native_session_id"] }

    var nativeThreadId: JSONValue? { storage["native_thread_id"] }

    var outputTokens: JSONValue? { storage["output_tokens"] }

    var reasoningTokens: JSONValue? { storage["reasoning_tokens"] }

    var scope: JSONValue? { storage["scope"] }

    var source: JSONValue? { storage["source"] }

    var tokenUsage: JSONValue? { storage["token_usage"] }

    var totalTokens: JSONValue? { storage["total_tokens"] }

    var usage: JSONValue? { storage["usage"] }

    var usedPercent: JSONValue? { storage["used_percent"] }
  }

  struct ControlError: Codable, Equatable {
    var status: Int?
    var code: ControlErrorCode
    var message: String
    var details: [String: String]?

    enum CodingKeys: String, CodingKey {
      case status
      case code
      case message
      case details
    }
  }

  enum ControlErrorCode: String, Codable, Equatable, Sendable {
    case controlErrorCapabilityDenied = "capability_denied"
    case controlErrorCapabilityMismatch = "capability_mismatch"
    case controlErrorActionUnknown = "control_action_unknown"
    case controlErrorAuthorizationRequired = "control_authorization_required"
    case controlErrorControllerRequired = "controller_device_required"
    case controlErrorHostUnavailable = "host_service_unavailable"
    case controlErrorInvalidParams = "invalid_params"
    case controlErrorRemoteHostRequired = "remote_host_required"
  }

  struct ControlErrorNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var code: JSONValue? { storage["code"] }

    var details: JSONValue? { storage["details"] }

    var message: JSONValue? { storage["message"] }

    var reason: JSONValue? { storage["reason"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }
  }

  struct ControlInterruptNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var requestId: JSONValue? { storage["request_id"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct ControlModelNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var method: JSONValue? { storage["method"] }

    var params: JSONValue? { storage["params"] }

    var source: JSONValue? { storage["source"] }
  }

  struct ControlNotificationNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var body: JSONValue? { storage["body"] }

    var notificationId: JSONValue? { storage["notification_id"] }

    var reason: JSONValue? { storage["reason"] }

    var source: JSONValue? { storage["source"] }

    var sourceEvent: JSONValue? { storage["source_event"] }

    var target: JSONValue? { storage["target"] }

    var title: JSONValue? { storage["title"] }

    var visibility: JSONValue? { storage["visibility"] }
  }

  struct ControlPairingApprovedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var controllerDeviceId: JSONValue? { storage["controller_device_id"] }

    var hostDeviceId: JSONValue? { storage["host_device_id"] }

    var requestId: JSONValue? { storage["request_id"] }

    var status: JSONValue? { storage["status"] }
  }

  struct ControlPairingDeniedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var controllerDeviceId: JSONValue? { storage["controller_device_id"] }

    var hostDeviceId: JSONValue? { storage["host_device_id"] }

    var requestId: JSONValue? { storage["request_id"] }

    var status: JSONValue? { storage["status"] }
  }

  struct ControlPairingRequestedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var capabilities: JSONValue? { storage["capabilities"] }

    var cloudRequestId: JSONValue? { storage["cloud_request_id"] }

    var controllerDeviceId: JSONValue? { storage["controller_device_id"] }

    var controllerDeviceKind: JSONValue? { storage["controller_device_kind"] }

    var controllerDeviceName: JSONValue? { storage["controller_device_name"] }

    var controllerPublicKeyFingerprint: JSONValue? { storage["controller_public_key_fingerprint"] }

    var hostDeviceId: JSONValue? { storage["host_device_id"] }

    var requestId: JSONValue? { storage["request_id"] }

    var scope: JSONValue? { storage["scope"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }
  }

  struct ControlRateLimitNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var limits: JSONValue? { storage["limits"] }

    var source: JSONValue? { storage["source"] }
  }

  struct ControlRawNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var error: JSONValue? { storage["error"] }

    var method: JSONValue? { storage["method"] }

    var normalized: JSONValue? { storage["normalized"] }

    var originalKind: JSONValue? { storage["original_kind"] }

    var raw: JSONValue? { storage["raw"] }

    var source: JSONValue? { storage["source"] }

    var subtype: JSONValue? { storage["subtype"] }

    var type: JSONValue? { storage["type"] }

    var value: JSONValue? { storage["value"] }
  }

  struct ControlRequest: Codable, Equatable {
    var requestId: String?
    var controllerDeviceId: String?
    var capability: ControlCapability
    var action: ControlAction
    var params: JSONValue?

    enum CodingKeys: String, CodingKey {
      case requestId = "request_id"
      case controllerDeviceId = "controller_device_id"
      case capability
      case action
      case params
    }
  }

  struct ControlResponse: Codable, Equatable {
    var requestId: String?
    var ok: Bool
    var result: JSONValue?
    var error: ControlError?

    enum CodingKeys: String, CodingKey {
      case requestId = "request_id"
      case ok
      case result
      case error
    }
  }

  struct ControlStatusNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var activeFlags: JSONValue? { storage["active_flags"] }

    var kind: JSONValue? { storage["kind"] }

    var message: JSONValue? { storage["message"] }

    var name: JSONValue? { storage["name"] }

    var permissionMode: JSONValue? { storage["permission_mode"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }
  }

  struct ControlSteerNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct ControlTerminalAttachedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var inputLeaseId: JSONValue? { storage["input_lease_id"] }

    var status: JSONValue? { storage["status"] }

    var terminalId: JSONValue? { storage["terminal_id"] }

    var viewerId: JSONValue? { storage["viewer_id"] }
  }

  struct ControlTerminalClosedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var reason: JSONValue? { storage["reason"] }

    var status: JSONValue? { storage["status"] }

    var terminalId: JSONValue? { storage["terminal_id"] }
  }

  struct ControlTerminalDetachedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var reason: JSONValue? { storage["reason"] }

    var released: JSONValue? { storage["released"] }

    var status: JSONValue? { storage["status"] }

    var terminalId: JSONValue? { storage["terminal_id"] }

    var viewerId: JSONValue? { storage["viewer_id"] }
  }

  struct ControlTerminalOpenedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var cwd: JSONValue? { storage["cwd"] }

    var shell: JSONValue? { storage["shell"] }

    var status: JSONValue? { storage["status"] }

    var terminalId: JSONValue? { storage["terminal_id"] }
  }

  struct ControlTrustGrantedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var capabilities: JSONValue? { storage["capabilities"] }

    var controllerDeviceId: JSONValue? { storage["controller_device_id"] }

    var hostDeviceId: JSONValue? { storage["host_device_id"] }

    var pairingRequestId: JSONValue? { storage["pairing_request_id"] }

    var scope: JSONValue? { storage["scope"] }
  }

  struct ControlTrustRevokedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var controllerDeviceId: JSONValue? { storage["controller_device_id"] }

    var hostDeviceId: JSONValue? { storage["host_device_id"] }

    var releasedTerminalWriters: JSONValue? { storage["released_terminal_writers"] }

    var revokedAt: JSONValue? { storage["revoked_at"] }
  }

  struct ControlWarningNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var attempt: JSONValue? { storage["attempt"] }

    var error: JSONValue? { storage["error"] }

    var errorStatus: JSONValue? { storage["error_status"] }

    var kind: JSONValue? { storage["kind"] }

    var maxRetries: JSONValue? { storage["max_retries"] }

    var message: JSONValue? { storage["message"] }

    var name: JSONValue? { storage["name"] }

    var retryDelayMs: JSONValue? { storage["retry_delay_ms"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }
  }

  struct CreateSessionRequest: Codable, Equatable {
    var workspaceId: String
    var agent: AgentKind?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case agent
    }
  }

  struct CreateWorkspaceRequest: Codable, Equatable {
    var name: String
    var target: String
    var agent: AgentKind?
    var localCwd: String?
    var ssh: SSHConfig?

    enum CodingKeys: String, CodingKey {
      case name
      case target
      case agent
      case localCwd = "local_cwd"
      case ssh
    }
  }

  struct DeviceIdentity: Codable, Equatable {
    var deviceId: String
    var deviceName: String
    var deviceKind: String
    var publicKey: String
    var publicKeyFingerprint: String
    var capabilities: [ControlCapability]
    var createdAt: String
    var updatedAt: String

    enum CodingKeys: String, CodingKey {
      case deviceId = "device_id"
      case deviceName = "device_name"
      case deviceKind = "device_kind"
      case publicKey = "public_key"
      case publicKeyFingerprint = "public_key_fingerprint"
      case capabilities
      case createdAt = "created_at"
      case updatedAt = "updated_at"
    }
  }

  struct EditLastUserMessageRequest: Codable, Equatable {
    var eventSeq: Int64
    var input: String
    var model: String
    var reasoningEffort: String
    var permissionMode: String

    enum CodingKeys: String, CodingKey {
      case eventSeq = "event_seq"
      case input
      case model
      case reasoningEffort = "reasoning_effort"
      case permissionMode = "permission_mode"
    }
  }

  struct EditableUserMessageView: Codable, Equatable {
    var eventSeq: Int64
    var text: String

    enum CodingKeys: String, CodingKey {
      case eventSeq = "event_seq"
      case text
    }
  }

  struct EventSubscriptionCancelParams: Codable, Equatable {
    var streamId: String

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
    }
  }

  struct EventSubscriptionCancelResult: Codable, Equatable {
    var streamId: String
    var cancelled: Bool

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case cancelled
    }
  }

  struct EventSubscriptionParams: Codable, Equatable {
    var workspaceId: String?
    var sessionId: String?
    var afterSeq: Int64?
    var replayLimit: Int?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case sessionId = "session_id"
      case afterSeq = "after_seq"
      case replayLimit = "replay_limit"
    }
  }

  struct EventSubscriptionResult: Codable, Equatable {
    var streamId: String
    var workspaceId: String?
    var sessionId: String?
    var afterSeq: Int64?
    var replayLimit: Int?

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case workspaceId = "workspace_id"
      case sessionId = "session_id"
      case afterSeq = "after_seq"
      case replayLimit = "replay_limit"
    }
  }

  struct EventWindowParams: Codable, Equatable {
    var workspaceId: String?
    var sessionId: String?
    var afterSeq: Int64?
    var beforeSeq: Int64?
    var limit: Int?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case sessionId = "session_id"
      case afterSeq = "after_seq"
      case beforeSeq = "before_seq"
      case limit
    }
  }

  struct ForkSessionRequest: Codable, Equatable {
    var eventSeq: Int64

    enum CodingKeys: String, CodingKey {
      case eventSeq = "event_seq"
    }
  }

  struct ForkSessionResponse: Codable, Equatable {
    var session: Session

    enum CodingKeys: String, CodingKey {
      case session
    }
  }

  struct HookCompletedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var exitCode: JSONValue? { storage["exit_code"] }

    var hookEventName: JSONValue? { storage["hook_event_name"] }

    var id: JSONValue? { storage["id"] }

    var name: JSONValue? { storage["name"] }

    var outcome: JSONValue? { storage["outcome"] }

    var output: JSONValue? { storage["output"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var stderr: JSONValue? { storage["stderr"] }

    var stdout: JSONValue? { storage["stdout"] }
  }

  struct HookProgressNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var exitCode: JSONValue? { storage["exit_code"] }

    var hookEventName: JSONValue? { storage["hook_event_name"] }

    var id: JSONValue? { storage["id"] }

    var name: JSONValue? { storage["name"] }

    var outcome: JSONValue? { storage["outcome"] }

    var output: JSONValue? { storage["output"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var stderr: JSONValue? { storage["stderr"] }

    var stdout: JSONValue? { storage["stdout"] }
  }

  struct HookStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var exitCode: JSONValue? { storage["exit_code"] }

    var hookEventName: JSONValue? { storage["hook_event_name"] }

    var id: JSONValue? { storage["id"] }

    var name: JSONValue? { storage["name"] }

    var outcome: JSONValue? { storage["outcome"] }

    var output: JSONValue? { storage["output"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var stderr: JSONValue? { storage["stderr"] }

    var stdout: JSONValue? { storage["stdout"] }
  }

  struct HostFileSystemBrowseParams: Codable, Equatable {
    var target: String
    var path: String?
    var ssh: SSHConfig?

    enum CodingKeys: String, CodingKey {
      case target
      case path
      case ssh
    }
  }

  struct HostFileSystemBrowseResult: Codable, Equatable {
    var target: String
    var platform: String
    var separator: String
    var path: String
    var parentPath: String?
    var roots: [HostFileSystemRoot]
    var entries: [HostFileSystemEntry]
    var truncated: Bool?

    enum CodingKeys: String, CodingKey {
      case target
      case platform
      case separator
      case path
      case parentPath = "parent_path"
      case roots
      case entries
      case truncated
    }
  }

  struct HostFileSystemEntry: Codable, Equatable {
    var name: String
    var path: String
    var kind: String
    var size: Int64?
    var modTime: String?

    enum CodingKeys: String, CodingKey {
      case name
      case path
      case kind
      case size
      case modTime = "mod_time"
    }
  }

  struct HostFileSystemRoot: Codable, Equatable {
    var id: String
    var label: String
    var path: String
    var kind: String

    enum CodingKeys: String, CodingKey {
      case id
      case label
      case path
      case kind
    }
  }

  struct HostInfo: Codable, Equatable {
    var identity: DeviceIdentity
    var platform: [String: String]
    var features: [String: JSONValue]
    var capabilities: [ControlCapability]

    enum CodingKeys: String, CodingKey {
      case identity
      case platform
      case features
      case capabilities
    }
  }

  struct HostSnapshotRequest: Codable, Equatable {
    var eventLimit: Int?
    var restoreOnLaunch: Bool?

    enum CodingKeys: String, CodingKey {
      case eventLimit = "event_limit"
      case restoreOnLaunch = "restore_on_launch"
    }
  }

  struct HostSnapshotResponse: Codable, Equatable {
    var host: HostInfo
    var agents: [String: JSONValue]?
    var workspaces: [Workspace]
    var sessions: [Session]
    var workspaceConnections: [WorkspaceConnection]?
    var events: [AstralEvent]
    var sessionViews: [SessionView]
    var initialSessionEvents: [AstralEvent]?
    var workbench: WorkbenchState?

    enum CodingKeys: String, CodingKey {
      case host
      case agents
      case workspaces
      case sessions
      case workspaceConnections = "workspace_connections"
      case events
      case sessionViews = "session_views"
      case initialSessionEvents = "initial_session_events"
      case workbench
    }
  }

  struct HostTrustListResult: Codable, Equatable {
    var grants: [TrustGrant]

    enum CodingKeys: String, CodingKey {
      case grants
    }
  }

  struct HostTrustRevokeParams: Codable, Equatable {
    var controllerDeviceId: String

    enum CodingKeys: String, CodingKey {
      case controllerDeviceId = "controller_device_id"
    }
  }

  struct HostTrustRevokeResult: Codable, Equatable {
    var controllerDeviceId: String
    var grant: TrustGrant
    var closedControlSessions: Int
    var releasedTerminalWriters: Int
    var revokedAt: String?

    enum CodingKeys: String, CodingKey {
      case controllerDeviceId = "controller_device_id"
      case grant
      case closedControlSessions = "closed_control_sessions"
      case releasedTerminalWriters = "released_terminal_writers"
      case revokedAt = "revoked_at"
    }
  }

  struct InputAttachment: Codable, Equatable {
    var id: String
    var kind: String
    var path: String
    var name: String
    var mimeType: String?
    var size: Int64?
    var detail: String?

    enum CodingKeys: String, CodingKey {
      case id
      case kind
      case path
      case name
      case mimeType = "mime_type"
      case size
      case detail
    }
  }

  struct InteractionActionView: Codable, Equatable {
    var id: String
    var label: String
    var description: String?
    var role: String?
    var requiresFeedback: Bool?

    enum CodingKeys: String, CodingKey {
      case id
      case label
      case description
      case role
      case requiresFeedback = "requires_feedback"
    }
  }

  struct InteractionDetailRow: Codable, Equatable {
    var key: String?
    var label: String
    var value: String
    var mono: Bool?

    enum CodingKeys: String, CodingKey {
      case key
      case label
      case value
      case mono
    }
  }

  struct InteractionRespondParams: Codable, Equatable {
    var interactionId: String
    var response: [String: JSONValue]

    enum CodingKeys: String, CodingKey {
      case interactionId = "interaction_id"
      case response
    }
  }

  struct MediaReadParams: Codable, Equatable {
    var sessionId: String
    var eventSeq: Int64
    var mediaId: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case mediaId = "media_id"
    }
  }

  struct MediaReadResult: Codable, Equatable {
    var sessionId: String
    var eventSeq: Int64
    var mediaId: String
    var kind: String
    var name: String
    var mimeType: String?
    var size: Int64?
    var contentBase64: String
    var download: Bool?

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case mediaId = "media_id"
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case contentBase64 = "content_base64"
      case download
    }
  }

  struct MediaStreamCancelParams: Codable, Equatable {
    var streamId: String

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
    }
  }

  struct MediaStreamCancelResult: Codable, Equatable {
    var streamId: String
    var cancelled: Bool

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case cancelled
    }
  }

  struct MediaStreamFrame: Codable, Equatable {
    var streamId: String
    var resumeToken: String?
    var requestId: String?
    var sessionId: String
    var eventSeq: Int64
    var mediaId: String
    var kind: String?
    var name: String?
    var mimeType: String?
    var size: Int64?
    var seq: Int64
    var offset: Int64
    var dataBase64: String?
    var final: Bool?
    var errorCode: String?
    var errorMessage: String?

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case resumeToken = "resume_token"
      case requestId = "request_id"
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case mediaId = "media_id"
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case seq
      case offset
      case dataBase64 = "data_base64"
      case final
      case errorCode = "error_code"
      case errorMessage = "error_message"
    }
  }

  struct MediaStreamParams: Codable, Equatable {
    var sessionId: String?
    var eventSeq: Int64?
    var mediaId: String?
    var resumeToken: String?
    var offset: Int64?
    var chunkSize: Int?

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case mediaId = "media_id"
      case resumeToken = "resume_token"
      case offset
      case chunkSize = "chunk_size"
    }
  }

  struct MediaStreamResult: Codable, Equatable {
    var streamId: String
    var resumeToken: String
    var sessionId: String
    var eventSeq: Int64
    var mediaId: String
    var kind: String
    var name: String
    var mimeType: String?
    var size: Int64?
    var offset: Int64
    var chunkSize: Int

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case resumeToken = "resume_token"
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case mediaId = "media_id"
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case offset
      case chunkSize = "chunk_size"
    }
  }

  struct MemoryCompactedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var metadata: JSONValue? { storage["metadata"] }

    var source: JSONValue? { storage["source"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct MemoryCompactingNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var message: JSONValue? { storage["message"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }
  }

  struct MessageAssistantNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var attachments: JSONValue? { storage["attachments"] }

    var forkProjection: JSONValue? { storage["fork_projection"] }

    var itemId: JSONValue? { storage["item_id"] }

    var media: JSONValue? { storage["media"] }

    var messageId: JSONValue? { storage["message_id"] }

    var nativeMessageUuid: JSONValue? { storage["native_message_uuid"] }

    var source: JSONValue? { storage["source"] }

    var sourceSeq: JSONValue? { storage["source_seq"] }

    var sourceSessionId: JSONValue? { storage["source_session_id"] }

    var suppressNotification: JSONValue? { storage["suppress_notification"] }

    var text: JSONValue? { storage["text"] }
  }

  struct MessageDeltaNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var messageId: JSONValue? { storage["message_id"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }
  }

  struct MessageMediaNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var detail: JSONValue? { storage["detail"] }

    var height: JSONValue? { storage["height"] }

    var itemId: JSONValue? { storage["item_id"] }

    var kind: JSONValue? { storage["kind"] }

    var mediaId: JSONValue? { storage["media_id"] }

    var mimeType: JSONValue? { storage["mime_type"] }

    var name: JSONValue? { storage["name"] }

    var path: JSONValue? { storage["path"] }

    var revisedPrompt: JSONValue? { storage["revised_prompt"] }

    var savedPath: JSONValue? { storage["saved_path"] }

    var size: JSONValue? { storage["size"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var width: JSONValue? { storage["width"] }
  }

  struct MessageStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var messageId: JSONValue? { storage["message_id"] }

    var source: JSONValue? { storage["source"] }
  }

  struct MessageUserNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var attachments: JSONValue? { storage["attachments"] }

    var forkProjection: JSONValue? { storage["fork_projection"] }

    var input: JSONValue? { storage["input"] }

    var media: JSONValue? { storage["media"] }

    var source: JSONValue? { storage["source"] }

    var sourceSeq: JSONValue? { storage["source_seq"] }

    var sourceSessionId: JSONValue? { storage["source_session_id"] }

    var suppressNotification: JSONValue? { storage["suppress_notification"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ModelInfo: Codable, Equatable {
    var id: String
    var label: String?
    var source: String?
    var slot: String?
    var defaultReasoningEffort: String?
    var supportedReasoningEfforts: [String]?
    var contextWindow: Int64?
    var maxContextWindow: Int64?
    var effectiveContextWindow: Int64?
    var effectiveContextWindowPercent: Double?

    enum CodingKeys: String, CodingKey {
      case id
      case label
      case source
      case slot
      case defaultReasoningEffort = "default_reasoning_effort"
      case supportedReasoningEfforts = "supported_reasoning_efforts"
      case contextWindow = "context_window"
      case maxContextWindow = "max_context_window"
      case effectiveContextWindow = "effective_context_window"
      case effectiveContextWindowPercent = "effective_context_window_percent"
    }
  }

  struct NativeSessionImportParams: Codable, Equatable {
    var workspaceId: String
    var sessionId: String

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case sessionId = "session_id"
    }
  }

  struct NativeSessionImportResponse: Codable, Equatable {
    var session: Session

    enum CodingKeys: String, CodingKey {
      case session
    }
  }

  struct NativeSessionListResponse: Codable, Equatable {
    var sessions: [Session]

    enum CodingKeys: String, CodingKey {
      case sessions
    }
  }

  struct NativeSessionRef: Codable, Equatable {
    var agent: AgentKind
    var localPath: String?
    var remotePath: String?
    var nativeSessionId: String?
    var nativeThreadId: String?
    var workspaceCwd: String?

    enum CodingKeys: String, CodingKey {
      case agent
      case localPath = "local_path"
      case remotePath = "remote_path"
      case nativeSessionId = "native_session_id"
      case nativeThreadId = "native_thread_id"
      case workspaceCwd = "workspace_cwd"
    }
  }

  struct NativeSessionsReadParams: Codable, Equatable {
    var workspaceId: String

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
    }
  }

  struct OkResult: Codable, Equatable {
    var ok: Bool

    enum CodingKeys: String, CodingKey {
      case ok
    }
  }

  struct PairingRequest: Codable, Equatable {
    var requestId: String
    var source: String?
    var cloudRequestId: String?
    var hostDeviceId: String
    var controllerDeviceId: String
    var controllerDeviceName: String?
    var controllerDeviceKind: String?
    var controllerPublicKey: String
    var controllerPublicKeyFingerprint: String
    var scope: String
    var status: String
    var capabilities: [String]
    var workspaceExecPolicy: String?
    var createdAt: String
    var updatedAt: String
    var resolvedAt: String?

    enum CodingKeys: String, CodingKey {
      case requestId = "request_id"
      case source
      case cloudRequestId = "cloud_request_id"
      case hostDeviceId = "host_device_id"
      case controllerDeviceId = "controller_device_id"
      case controllerDeviceName = "controller_device_name"
      case controllerDeviceKind = "controller_device_kind"
      case controllerPublicKey = "controller_public_key"
      case controllerPublicKeyFingerprint = "controller_public_key_fingerprint"
      case scope
      case status
      case capabilities
      case workspaceExecPolicy = "workspace_exec_policy"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
      case resolvedAt = "resolved_at"
    }
  }

  struct PairingRequestListResult: Codable, Equatable {
    var requests: [PairingRequest]

    enum CodingKeys: String, CodingKey {
      case requests
    }
  }

  struct PairingRequestResolveParams: Codable, Equatable {
    var requestId: String

    enum CodingKeys: String, CodingKey {
      case requestId = "request_id"
    }
  }

  struct PairingRequestResolveResult: Codable, Equatable {
    var request: PairingRequest
    var grant: TrustGrant?

    enum CodingKeys: String, CodingKey {
      case request
      case grant
    }
  }

  struct PendingInteractionView: Codable, Equatable {
    var id: String
    var kind: String
    var title: String
    var detailRows: [InteractionDetailRow]?
    var actions: [InteractionActionView]
    var form: [String: JSONValue]?

    enum CodingKeys: String, CodingKey {
      case id
      case kind
      case title
      case detailRows = "detail_rows"
      case actions
      case form
    }
  }

  struct PlanDeltaNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }
  }

  struct PlanUpdatedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var plan: JSONValue? { storage["plan"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct QueueCancelledNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var `internal`: JSONValue? { storage["internal"] }

    var queueId: JSONValue? { storage["queue_id"] }

    var reason: JSONValue? { storage["reason"] }

    var text: JSONValue? { storage["text"] }
  }

  struct QueueControlParams: Codable, Equatable {
    var sessionId: String
    var queueId: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case queueId = "queue_id"
    }
  }

  struct QueueControlResult: Codable, Equatable {
    var ok: Bool
    var queueId: String

    enum CodingKeys: String, CodingKey {
      case ok
      case queueId = "queue_id"
    }
  }

  struct QueueDequeuedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var `internal`: JSONValue? { storage["internal"] }

    var queueId: JSONValue? { storage["queue_id"] }

    var text: JSONValue? { storage["text"] }
  }

  struct QueueFailedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var `internal`: JSONValue? { storage["internal"] }

    var message: JSONValue? { storage["message"] }

    var queueId: JSONValue? { storage["queue_id"] }

    var reason: JSONValue? { storage["reason"] }

    var text: JSONValue? { storage["text"] }
  }

  struct QueueQueuedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var `internal`: JSONValue? { storage["internal"] }

    var queueId: JSONValue? { storage["queue_id"] }

    var sessionId: JSONValue? { storage["session_id"] }

    var text: JSONValue? { storage["text"] }
  }

  struct QueueSteeredNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var `internal`: JSONValue? { storage["internal"] }

    var queueId: JSONValue? { storage["queue_id"] }

    var text: JSONValue? { storage["text"] }
  }

  struct QueuedInputView: Codable, Equatable {
    var id: String
    var sessionId: String
    var text: String

    enum CodingKeys: String, CodingKey {
      case id
      case sessionId = "session_id"
      case text
    }
  }

  struct ReasoningCompletedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var summary: JSONValue? { storage["summary"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ReasoningDeltaNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ReasoningStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var itemId: JSONValue? { storage["item_id"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var summary: JSONValue? { storage["summary"] }

    var text: JSONValue? { storage["text"] }
  }

  struct SSHConfig: Codable, Equatable {
    var endpoint: String
    var port: Int
    var remoteCwd: String

    enum CodingKeys: String, CodingKey {
      case endpoint
      case port
      case remoteCwd = "remote_cwd"
    }
  }

  struct Session: Codable, Equatable {
    var id: String
    var workspaceId: String
    var agent: AgentKind
    var title: String?
    var status: String
    var source: SessionSource?
    var nativeRef: NativeSessionRef?
    var managedByAstralops: Bool?
    var nativeSessionId: String?
    var nativeThreadId: String?
    var forkedFromSessionId: String?
    var forkedFromEventSeq: Int64?
    var forkedFromNativeAnchor: String?
    var forkedFromTitle: String?
    var createdAt: String
    var updatedAt: String

    enum CodingKeys: String, CodingKey {
      case id
      case workspaceId = "workspace_id"
      case agent
      case title
      case status
      case source
      case nativeRef = "native_ref"
      case managedByAstralops = "managed_by_astralops"
      case nativeSessionId = "native_session_id"
      case nativeThreadId = "native_thread_id"
      case forkedFromSessionId = "forked_from_session_id"
      case forkedFromEventSeq = "forked_from_event_seq"
      case forkedFromNativeAnchor = "forked_from_native_anchor"
      case forkedFromTitle = "forked_from_title"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
    }
  }

  struct SessionCommand: Codable, Equatable {
    var id: String
    var title: String
    var description: String?
    var icon: String?
    var kind: String
    var enabled: Bool
    var disabledReason: String?
    var agent: AgentKind?
    var clientAction: String?
    var payload: [String: JSONValue]?

    enum CodingKeys: String, CodingKey {
      case id
      case title
      case description
      case icon
      case kind
      case enabled
      case disabledReason = "disabled_reason"
      case agent
      case clientAction = "client_action"
      case payload
    }
  }

  enum SessionCommandKind: String, Codable, Equatable, Sendable {
    case action = "action"
    case client = "client"
    case prompt = "prompt"
  }

  struct SessionCommandListResponse: Codable, Equatable {
    var commands: [SessionCommand]

    enum CodingKeys: String, CodingKey {
      case commands
    }
  }

  struct SessionCommandRequest: Codable, Equatable {
    var args: [String: JSONValue]?

    enum CodingKeys: String, CodingKey {
      case args
    }
  }

  struct SessionCommandResponse: Codable, Equatable {
    var ok: Bool
    var queued: Bool?
    var queueId: String?

    enum CodingKeys: String, CodingKey {
      case ok
      case queued
      case queueId = "queue_id"
    }
  }

  struct SessionDeleteParams: Codable, Equatable {
    var sessionId: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
    }
  }

  struct SessionDeleteResult: Codable, Equatable {
    var ok: Bool
    var sessionId: String

    enum CodingKeys: String, CodingKey {
      case ok
      case sessionId = "session_id"
    }
  }

  struct SessionDeletedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var message: JSONValue? { storage["message"] }

    var reason: JSONValue? { storage["reason"] }

    var sessionId: JSONValue? { storage["session_id"] }
  }

  struct SessionEditParams: Codable, Equatable {
    var sessionId: String
    var eventSeq: Int64
    var input: String
    var model: String?
    var reasoningEffort: String?
    var permissionMode: String?

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case eventSeq = "event_seq"
      case input
      case model
      case reasoningEffort = "reasoning_effort"
      case permissionMode = "permission_mode"
    }
  }

  struct SessionForkControlParams: Codable, Equatable {
    var sessionId: String
    var eventSeq: Int64

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case eventSeq = "event_seq"
    }
  }

  struct SessionForkResponse: Codable, Equatable {
    var session: Session

    enum CodingKeys: String, CodingKey {
      case session
    }
  }

  struct SessionInputControlParams: Codable, Equatable {
    var sessionId: String
    var input: String
    var attachments: [ControlAttachmentHandle]?
    var model: String?
    var reasoningEffort: String?
    var permissionMode: String?

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case input
      case attachments
      case model
      case reasoningEffort = "reasoning_effort"
      case permissionMode = "permission_mode"
    }
  }

  struct SessionInputRequest: Codable, Equatable {
    var input: String
    var attachments: [ControlAttachmentHandle]?
    var model: String?
    var reasoningEffort: String?
    var permissionMode: String?

    enum CodingKeys: String, CodingKey {
      case input
      case attachments
      case model
      case reasoningEffort = "reasoning_effort"
      case permissionMode = "permission_mode"
    }
  }

  struct SessionInputResult: Codable, Equatable {
    var ok: Bool
    var mode: String
    var queued: Bool?
    var steered: Bool?
    var queueId: String?

    enum CodingKeys: String, CodingKey {
      case ok
      case mode
      case queued
      case steered
      case queueId = "queue_id"
    }
  }

  struct SessionLinkState: Codable, Equatable {
    var sessionId: String
    var workspaceId: String
    var source: SessionSource
    var nativeRef: NativeSessionRef?
    var managedByAstralops: Bool
    var createdAt: String
    var updatedAt: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
      case workspaceId = "workspace_id"
      case source
      case nativeRef = "native_ref"
      case managedByAstralops = "managed_by_astralops"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
    }
  }

  struct SessionNativeNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var aiTitle: JSONValue? { storage["aiTitle"] }

    var customTitle: JSONValue? { storage["customTitle"] }

    var firstPrompt: JSONValue? { storage["firstPrompt"] }

    var name: JSONValue? { storage["name"] }

    var nativeSessionId: JSONValue? { storage["native_session_id"] }

    var nativeThreadId: JSONValue? { storage["native_thread_id"] }

    var preview: JSONValue? { storage["preview"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var subtype: JSONValue? { storage["subtype"] }

    var summary: JSONValue? { storage["summary"] }

    var title: JSONValue? { storage["title"] }

    var type: JSONValue? { storage["type"] }
  }

  struct SessionReferenceParams: Codable, Equatable {
    var sessionId: String

    enum CodingKeys: String, CodingKey {
      case sessionId = "session_id"
    }
  }

  enum SessionSource: String, Codable, Equatable, Sendable {
    case discovered = "discovered"
    case legacyUnlinked = "legacy_unlinked"
    case linked = "linked"
    case managed = "managed"
  }

  struct SessionStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var agent: JSONValue? { storage["agent"] }

    var createdAt: JSONValue? { storage["created_at"] }

    var forkedFromEventSeq: JSONValue? { storage["forked_from_event_seq"] }

    var forkedFromSessionId: JSONValue? { storage["forked_from_session_id"] }

    var forkedFromTitle: JSONValue? { storage["forked_from_title"] }

    var id: JSONValue? { storage["id"] }

    var managedByAstralops: JSONValue? { storage["managed_by_astralops"] }

    var nativeRef: JSONValue? { storage["native_ref"] }

    var nativeSessionId: JSONValue? { storage["native_session_id"] }

    var nativeThreadId: JSONValue? { storage["native_thread_id"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var title: JSONValue? { storage["title"] }

    var updatedAt: JSONValue? { storage["updated_at"] }

    var workspaceId: JSONValue? { storage["workspace_id"] }
  }

  enum SessionStatus: String, Codable, Equatable, Sendable {
    case failed = "failed"
    case idle = "idle"
    case reconnecting = "reconnecting"
    case requiresAction = "requires_action"
    case running = "running"
  }

  struct SessionUpdatedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var description: JSONValue? { storage["description"] }

    var name: JSONValue? { storage["name"] }

    var recentAction: JSONValue? { storage["recent_action"] }

    var source: JSONValue? { storage["source"] }

    var subtype: JSONValue? { storage["subtype"] }

    var summarizesUuid: JSONValue? { storage["summarizes_uuid"] }

    var summary: JSONValue? { storage["summary"] }

    var threadName: JSONValue? { storage["thread_name"] }

    var title: JSONValue? { storage["title"] }

    var type: JSONValue? { storage["type"] }
  }

  struct SessionView: Codable, Equatable {
    var session: Session
    var title: String?
    var status: String
    var pendingInteraction: PendingInteractionView?
    var queuedInputs: [QueuedInputView]?
    var editableUserMessage: EditableUserMessageView?

    enum CodingKeys: String, CodingKey {
      case session
      case title
      case status
      case pendingInteraction = "pending_interaction"
      case queuedInputs = "queued_inputs"
      case editableUserMessage = "editable_user_message"
    }
  }

  struct SessionsReadParams: Codable, Equatable {
    var workspaceId: String?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
    }
  }

  struct TerminalAckResult: Codable, Equatable {
    var terminalId: String
    var status: String
    var outputSeq: Int64
    var writerDeviceId: String?
    var canInput: Bool?

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case status
      case outputSeq = "output_seq"
      case writerDeviceId = "writer_device_id"
      case canInput = "can_input"
    }
  }

  struct TerminalAttachParams: Codable, Equatable {
    var terminalId: String
    var afterSeq: Int64?

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case afterSeq = "after_seq"
    }
  }

  struct TerminalAttachResult: Codable, Equatable {
    var terminalId: String
    var workspaceId: String
    var target: String
    var status: String
    var viewerDeviceId: String
    var viewerId: String
    var inputLeaseId: String
    var connectionId: String
    var writerDeviceId: String?
    var outputSeq: Int64
    var canInput: Bool

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case workspaceId = "workspace_id"
      case target
      case status
      case viewerDeviceId = "viewer_device_id"
      case viewerId = "viewer_id"
      case inputLeaseId = "input_lease_id"
      case connectionId = "connection_id"
      case writerDeviceId = "writer_device_id"
      case outputSeq = "output_seq"
      case canInput = "can_input"
    }
  }

  struct TerminalCloseParams: Codable, Equatable {
    var terminalId: String

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
    }
  }

  struct TerminalDetachParams: Codable, Equatable {
    var terminalId: String

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
    }
  }

  struct TerminalHeartbeatAckParams: Codable, Equatable {
    var terminalId: String
    var viewerId: String
    var inputLeaseId: String
    var heartbeatSeq: Int64
    var renderedSeq: Int64?

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case viewerId = "viewer_id"
      case inputLeaseId = "input_lease_id"
      case heartbeatSeq = "heartbeat_seq"
      case renderedSeq = "rendered_seq"
    }
  }

  struct TerminalInputParams: Codable, Equatable {
    var terminalId: String
    var viewerId: String?
    var inputLeaseId: String?
    var data: String?

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case viewerId = "viewer_id"
      case inputLeaseId = "input_lease_id"
      case data
    }
  }

  struct TerminalOpenParams: Codable, Equatable {
    var workspaceId: String
    var cwd: String?
    var cols: Int?
    var rows: Int?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case cwd
      case cols
      case rows
    }
  }

  struct TerminalOpenResult: Codable, Equatable {
    var terminalId: String
    var workspaceId: String
    var target: String
    var shell: String?
    var cwd: String?
    var status: String
    var writerDeviceId: String?
    var outputSeq: Int64

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case workspaceId = "workspace_id"
      case target
      case shell
      case cwd
      case status
      case writerDeviceId = "writer_device_id"
      case outputSeq = "output_seq"
    }
  }

  struct TerminalResizeParams: Codable, Equatable {
    var terminalId: String
    var viewerId: String?
    var inputLeaseId: String?
    var cols: Int
    var rows: Int

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case viewerId = "viewer_id"
      case inputLeaseId = "input_lease_id"
      case cols
      case rows
    }
  }

  struct TerminalTab: Codable, Equatable {
    var terminalId: String
    var workspaceId: String
    var agent: String
    var target: String
    var shell: String?
    var cwd: String?
    var status: String
    var writerDeviceId: String?
    var outputSeq: Int64
    var createdAt: String
    var updatedAt: String

    enum CodingKeys: String, CodingKey {
      case terminalId = "terminal_id"
      case workspaceId = "workspace_id"
      case agent
      case target
      case shell
      case cwd
      case status
      case writerDeviceId = "writer_device_id"
      case outputSeq = "output_seq"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
    }
  }

  struct ToolCompletedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var category: JSONValue? { storage["category"] }

    var command: JSONValue? { storage["command"] }

    var content: JSONValue? { storage["content"] }

    var cwd: JSONValue? { storage["cwd"] }

    var filePath: JSONValue? { storage["file_path"] }

    var hidden: JSONValue? { storage["hidden"] }

    var id: JSONValue? { storage["id"] }

    var isError: JSONValue? { storage["is_error"] }

    var itemId: JSONValue? { storage["item_id"] }

    var name: JSONValue? { storage["name"] }

    var path: JSONValue? { storage["path"] }

    var result: JSONValue? { storage["result"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var text: JSONValue? { storage["text"] }

    var visibility: JSONValue? { storage["visibility"] }
  }

  struct ToolDiffNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var diff: JSONValue? { storage["diff"] }

    var itemId: JSONValue? { storage["item_id"] }

    var patch: JSONValue? { storage["patch"] }

    var source: JSONValue? { storage["source"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct ToolOutputDeltaNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var category: JSONValue? { storage["category"] }

    var id: JSONValue? { storage["id"] }

    var itemId: JSONValue? { storage["item_id"] }

    var name: JSONValue? { storage["name"] }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ToolProgressNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var category: JSONValue? { storage["category"] }

    var elapsedTimeSeconds: JSONValue? { storage["elapsed_time_seconds"] }

    var id: JSONValue? { storage["id"] }

    var name: JSONValue? { storage["name"] }

    var parentToolUseId: JSONValue? { storage["parent_tool_use_id"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var taskId: JSONValue? { storage["task_id"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ToolStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var category: JSONValue? { storage["category"] }

    var command: JSONValue? { storage["command"] }

    var cwd: JSONValue? { storage["cwd"] }

    var effectiveCommand: JSONValue? { storage["effective_command"] }

    var id: JSONValue? { storage["id"] }

    var input: JSONValue? { storage["input"] }

    var itemId: JSONValue? { storage["item_id"] }

    var name: JSONValue? { storage["name"] }

    var nativeCommand: JSONValue? { storage["native_command"] }

    var params: JSONValue? { storage["params"] }

    var remoteCommand: JSONValue? { storage["remote_command"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var text: JSONValue? { storage["text"] }
  }

  struct ToolTodoNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var source: JSONValue? { storage["source"] }

    var text: JSONValue? { storage["text"] }

    var todos: JSONValue? { storage["todos"] }
  }

  struct TrustGrant: Codable, Equatable {
    var hostDeviceId: String
    var controllerDeviceId: String
    var controllerDeviceName: String?
    var controllerPublicKey: String?
    var controllerPublicKeyFingerprint: String?
    var scope: String
    var status: String
    var capabilities: [String]
    var workspaceExecPolicy: String?
    var createdAt: String
    var updatedAt: String
    var revokedAt: String?

    enum CodingKeys: String, CodingKey {
      case hostDeviceId = "host_device_id"
      case controllerDeviceId = "controller_device_id"
      case controllerDeviceName = "controller_device_name"
      case controllerPublicKey = "controller_public_key"
      case controllerPublicKeyFingerprint = "controller_public_key_fingerprint"
      case scope
      case status
      case capabilities
      case workspaceExecPolicy = "workspace_exec_policy"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
      case revokedAt = "revoked_at"
    }
  }

  struct TurnCancelledNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var reason: JSONValue? { storage["reason"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct TurnCompletedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var durationMs: JSONValue? { storage["duration_ms"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct TurnFailedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var message: JSONValue? { storage["message"] }

    var reason: JSONValue? { storage["reason"] }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct TurnReplacedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var endSeq: JSONValue? { storage["end_seq"] }

    var fromSeq: JSONValue? { storage["from_seq"] }

    var replacementSessionId: JSONValue? { storage["replacement_session_id"] }

    var source: JSONValue? { storage["source"] }

    var sourceSessionId: JSONValue? { storage["source_session_id"] }

    var startSeq: JSONValue? { storage["start_seq"] }

    var status: JSONValue? { storage["status"] }

    var toSeq: JSONValue? { storage["to_seq"] }
  }

  struct TurnStartedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var source: JSONValue? { storage["source"] }

    var status: JSONValue? { storage["status"] }

    var turnId: JSONValue? { storage["turn_id"] }
  }

  struct WorkbenchPanel: Codable, Equatable {
    var id: String
    var kind: String
    var state: [String: JSONValue]?
    var updatedAt: String?

    enum CodingKeys: String, CodingKey {
      case id
      case kind
      case state
      case updatedAt = "updated_at"
    }
  }

  struct WorkbenchState: Codable, Equatable {
    var version: Int64
    var updatedAt: String
    var agents: [String: JSONValue]?
    var workspaces: [String: Workspace]
    var sessions: [String: Session]
    var sessionViews: [String: SessionView]
    var workspaceConnections: [String: WorkspaceConnection]
    var terminalTabs: [String: TerminalTab]
    var panels: [String: WorkbenchPanel]

    enum CodingKeys: String, CodingKey {
      case version
      case updatedAt = "updated_at"
      case agents
      case workspaces
      case sessions
      case sessionViews = "session_views"
      case workspaceConnections = "workspace_connections"
      case terminalTabs = "terminal_tabs"
      case panels
    }
  }

  struct Workspace: Codable, Equatable {
    var id: String
    var name: String
    var target: String
    var agent: AgentKind
    var localProjectionRoot: String
    var localCwd: String?
    var ssh: SSHConfig?
    var nativeSessionId: String?
    var nativeThreadId: String?
    var createdAt: String?
    var updatedAt: String?

    enum CodingKeys: String, CodingKey {
      case id
      case name
      case target
      case agent
      case localProjectionRoot = "local_projection_root"
      case localCwd = "local_cwd"
      case ssh
      case nativeSessionId = "native_session_id"
      case nativeThreadId = "native_thread_id"
      case createdAt = "created_at"
      case updatedAt = "updated_at"
    }
  }

  struct WorkspaceConnection: Codable, Equatable {
    var workspaceId: String
    var target: String
    var status: String
    var endpoint: String?
    var port: Int?
    var remoteCwd: String?
    var remoteUser: String?
    var remoteHost: String?
    var remoteOs: String?
    var remoteArch: String?
    var remoteShell: String?
    var displayCwd: String?
    var helperPath: String?
    var helperStatus: String?
    var capabilities: [String: JSONValue]?
    var message: String?
    var retryAttempt: Int?
    var retryMax: Int?
    var updatedAt: String
    var raw: [String: JSONValue]?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case status
      case endpoint
      case port
      case remoteCwd = "remote_cwd"
      case remoteUser = "remote_user"
      case remoteHost = "remote_host"
      case remoteOs = "remote_os"
      case remoteArch = "remote_arch"
      case remoteShell = "remote_shell"
      case displayCwd = "display_cwd"
      case helperPath = "helper_path"
      case helperStatus = "helper_status"
      case capabilities
      case message
      case retryAttempt = "retry_attempt"
      case retryMax = "retry_max"
      case updatedAt = "updated_at"
      case raw
    }
  }

  struct WorkspaceConnectionNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var capabilities: JSONValue? { storage["capabilities"] }

    var displayCwd: JSONValue? { storage["display_cwd"] }

    var endpoint: JSONValue? { storage["endpoint"] }

    var helperPath: JSONValue? { storage["helper_path"] }

    var helperStatus: JSONValue? { storage["helper_status"] }

    var message: JSONValue? { storage["message"] }

    var port: JSONValue? { storage["port"] }

    var remoteArch: JSONValue? { storage["remote_arch"] }

    var remoteCwd: JSONValue? { storage["remote_cwd"] }

    var remoteHost: JSONValue? { storage["remote_host"] }

    var remoteOs: JSONValue? { storage["remote_os"] }

    var remoteShell: JSONValue? { storage["remote_shell"] }

    var remoteUser: JSONValue? { storage["remote_user"] }

    var retryAttempt: JSONValue? { storage["retry_attempt"] }

    var retryMax: JSONValue? { storage["retry_max"] }

    var status: JSONValue? { storage["status"] }

    var target: JSONValue? { storage["target"] }

    var updatedAt: JSONValue? { storage["updated_at"] }

    var workspaceId: JSONValue? { storage["workspace_id"] }
  }

  struct WorkspaceCreatedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var agent: JSONValue? { storage["agent"] }

    var createdAt: JSONValue? { storage["created_at"] }

    var id: JSONValue? { storage["id"] }

    var localCwd: JSONValue? { storage["local_cwd"] }

    var localProjectionRoot: JSONValue? { storage["local_projection_root"] }

    var name: JSONValue? { storage["name"] }

    var ssh: JSONValue? { storage["ssh"] }

    var target: JSONValue? { storage["target"] }

    var updatedAt: JSONValue? { storage["updated_at"] }
  }

  struct WorkspaceExecParams: Codable, Equatable {
    var workspaceId: String
    var command: String
    var cwd: String
    var timeoutMs: Int

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case command
      case cwd
      case timeoutMs = "timeout_ms"
    }
  }

  struct WorkspaceExecResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var command: String
    var cwd: String
    var approvalPolicy: String
    var exitCode: Int
    var stdout: String
    var stderr: String
    var output: String?
    var stdoutTruncated: Bool?
    var stderrTruncated: Bool?
    var outputTruncated: Bool?
    var outputBytesLimit: Int?
    var durationMs: Int64
    var failure: String?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case command
      case cwd
      case approvalPolicy = "approval_policy"
      case exitCode = "exit_code"
      case stdout
      case stderr
      case output
      case stdoutTruncated = "stdout_truncated"
      case stderrTruncated = "stderr_truncated"
      case outputTruncated = "output_truncated"
      case outputBytesLimit = "output_bytes_limit"
      case durationMs = "duration_ms"
      case failure
    }
  }

  struct WorkspaceFileEntry: Codable, Equatable {
    var name: String
    var path: String
    var kind: String
    var size: Int64?
    var modTime: String?

    enum CodingKeys: String, CodingKey {
      case name
      case path
      case kind
      case size
      case modTime = "mod_time"
    }
  }

  struct WorkspaceFileStreamCancelParams: Codable, Equatable {
    var streamId: String

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
    }
  }

  struct WorkspaceFileStreamCancelResult: Codable, Equatable {
    var streamId: String
    var cancelled: Bool

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case cancelled
    }
  }

  struct WorkspaceFileStreamFrame: Codable, Equatable {
    var streamId: String
    var requestId: String?
    var workspaceId: String
    var target: String
    var path: String
    var kind: String?
    var name: String?
    var mimeType: String?
    var size: Int64?
    var seq: Int64
    var offset: Int64
    var dataBase64: String?
    var final: Bool?
    var errorCode: String?
    var errorMessage: String?

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case requestId = "request_id"
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case seq
      case offset
      case dataBase64 = "data_base64"
      case final
      case errorCode = "error_code"
      case errorMessage = "error_message"
    }
  }

  struct WorkspaceFileStreamResult: Codable, Equatable {
    var streamId: String
    var workspaceId: String
    var target: String
    var path: String
    var kind: String
    var name: String?
    var mimeType: String?
    var size: Int64?
    var offset: Int64
    var chunkSize: Int

    enum CodingKeys: String, CodingKey {
      case streamId = "stream_id"
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case name
      case mimeType = "mime_type"
      case size
      case offset
      case chunkSize = "chunk_size"
    }
  }

  struct WorkspaceFileTextEdit: Codable, Equatable {
    var oldString: String
    var newString: String
    var replaceAll: Bool?

    enum CodingKeys: String, CodingKey {
      case oldString = "old_string"
      case newString = "new_string"
      case replaceAll = "replace_all"
    }
  }

  struct WorkspaceFilesApplyPatchParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var edits: [WorkspaceFileTextEdit]

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case edits
    }
  }

  struct WorkspaceFilesApplyPatchResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var path: String
    var kind: String
    var size: Int64
    var appliedEdits: Int
    var structuredPatch: [[String: JSONValue]]?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case size
      case appliedEdits = "applied_edits"
      case structuredPatch = "structured_patch"
    }
  }

  struct WorkspaceFilesDeleteParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var recursive: Bool?
    var force: Bool?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case recursive
      case force
    }
  }

  struct WorkspaceFilesDeleteResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var path: String
    var kind: String
    var removed: Bool

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case removed
    }
  }

  struct WorkspaceFilesMoveParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var destinationPath: String
    var overwrite: Bool?
    var createParents: Bool?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case destinationPath = "destination_path"
      case overwrite
      case createParents = "create_parents"
    }
  }

  struct WorkspaceFilesMoveResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var fromPath: String
    var toPath: String
    var kind: String
    var size: Int64?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case fromPath = "from_path"
      case toPath = "to_path"
      case kind
      case size
    }
  }

  struct WorkspaceFilesReadParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var mode: String
    var maxBytes: Int64

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case mode
      case maxBytes = "max_bytes"
    }
  }

  struct WorkspaceFilesReadResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var path: String
    var kind: String
    var name: String?
    var size: Int64?
    var modTime: String?
    var mimeType: String?
    var contentBase64: String?
    var entries: [WorkspaceFileEntry]?
    var truncated: Bool?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case name
      case size
      case modTime = "mod_time"
      case mimeType = "mime_type"
      case contentBase64 = "content_base64"
      case entries
      case truncated
    }
  }

  struct WorkspaceFilesStreamParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var offset: Int64?
    var chunkSize: Int?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case offset
      case chunkSize = "chunk_size"
    }
  }

  struct WorkspaceFilesWriteParams: Codable, Equatable {
    var workspaceId: String
    var path: String
    var content: String
    var contentBase64: String
    var createParents: Bool?

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case path
      case content
      case contentBase64 = "content_base64"
      case createParents = "create_parents"
    }
  }

  struct WorkspaceFilesWriteResult: Codable, Equatable {
    var workspaceId: String
    var target: String
    var path: String
    var kind: String
    var size: Int64

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
      case target
      case path
      case kind
      case size
    }
  }

  struct WorkspaceReferenceParams: Codable, Equatable {
    var workspaceId: String

    enum CodingKeys: String, CodingKey {
      case workspaceId = "workspace_id"
    }
  }

  struct WorkspaceRemovedNormalized: Codable, Equatable {
    var storage: [String: JSONValue]

    init(_ value: [String: JSONValue] = [:]) {
      self.storage = value
    }

    init(from decoder: Decoder) throws {
      storage = try [String: JSONValue](from: decoder)
    }

    func encode(to encoder: Encoder) throws {
      try storage.encode(to: encoder)
    }

    var id: JSONValue? { storage["id"] }

    var reason: JSONValue? { storage["reason"] }

    var workspaceId: JSONValue? { storage["workspace_id"] }
  }

  enum WorkspaceTarget: String, Codable, Equatable, Sendable {
    case local = "local"
    case sSH = "ssh"
  }

  enum AstralNormalizedPayload: Codable, Equatable {
    case approvalRequested(ApprovalRequestedNormalized)
    case approvalResolved(ApprovalResolvedNormalized)
    case approvalResponded(ApprovalRespondedNormalized)
    case askRequested(AskRequestedNormalized)
    case askResolved(AskResolvedNormalized)
    case controlContext(ControlContextNormalized)
    case controlError(ControlErrorNormalized)
    case controlInterrupt(ControlInterruptNormalized)
    case controlModel(ControlModelNormalized)
    case controlNotification(ControlNotificationNormalized)
    case controlPairingApproved(ControlPairingApprovedNormalized)
    case controlPairingDenied(ControlPairingDeniedNormalized)
    case controlPairingRequested(ControlPairingRequestedNormalized)
    case controlRateLimit(ControlRateLimitNormalized)
    case controlRaw(ControlRawNormalized)
    case controlStatus(ControlStatusNormalized)
    case controlSteer(ControlSteerNormalized)
    case controlTerminalAttached(ControlTerminalAttachedNormalized)
    case controlTerminalClosed(ControlTerminalClosedNormalized)
    case controlTerminalDetached(ControlTerminalDetachedNormalized)
    case controlTerminalOpened(ControlTerminalOpenedNormalized)
    case controlTrustGranted(ControlTrustGrantedNormalized)
    case controlTrustRevoked(ControlTrustRevokedNormalized)
    case controlWarning(ControlWarningNormalized)
    case hookCompleted(HookCompletedNormalized)
    case hookProgress(HookProgressNormalized)
    case hookStarted(HookStartedNormalized)
    case memoryCompacted(MemoryCompactedNormalized)
    case memoryCompacting(MemoryCompactingNormalized)
    case messageAssistant(MessageAssistantNormalized)
    case messageDelta(MessageDeltaNormalized)
    case messageMedia(MessageMediaNormalized)
    case messageStarted(MessageStartedNormalized)
    case messageUser(MessageUserNormalized)
    case planDelta(PlanDeltaNormalized)
    case planUpdated(PlanUpdatedNormalized)
    case queueCancelled(QueueCancelledNormalized)
    case queueDequeued(QueueDequeuedNormalized)
    case queueFailed(QueueFailedNormalized)
    case queueQueued(QueueQueuedNormalized)
    case queueSteered(QueueSteeredNormalized)
    case reasoningCompleted(ReasoningCompletedNormalized)
    case reasoningDelta(ReasoningDeltaNormalized)
    case reasoningStarted(ReasoningStartedNormalized)
    case sessionDeleted(SessionDeletedNormalized)
    case sessionNative(SessionNativeNormalized)
    case sessionStarted(SessionStartedNormalized)
    case sessionUpdated(SessionUpdatedNormalized)
    case toolCompleted(ToolCompletedNormalized)
    case toolDiff(ToolDiffNormalized)
    case toolOutputDelta(ToolOutputDeltaNormalized)
    case toolProgress(ToolProgressNormalized)
    case toolStarted(ToolStartedNormalized)
    case toolTodo(ToolTodoNormalized)
    case turnCancelled(TurnCancelledNormalized)
    case turnCompleted(TurnCompletedNormalized)
    case turnFailed(TurnFailedNormalized)
    case turnReplaced(TurnReplacedNormalized)
    case turnStarted(TurnStartedNormalized)
    case workspaceConnection(WorkspaceConnectionNormalized)
    case workspaceCreated(WorkspaceCreatedNormalized)
    case workspaceRemoved(WorkspaceRemovedNormalized)

    static func decode<K: CodingKey>(kind: AstralEventKind, from container: KeyedDecodingContainer<K>, forKey key: K) throws -> AstralNormalizedPayload {
      switch kind {
      case .approvalRequested:
        return .approvalRequested(try container.decode(ApprovalRequestedNormalized.self, forKey: key))
      case .approvalResolved:
        return .approvalResolved(try container.decode(ApprovalResolvedNormalized.self, forKey: key))
      case .approvalResponded:
        return .approvalResponded(try container.decode(ApprovalRespondedNormalized.self, forKey: key))
      case .askRequested:
        return .askRequested(try container.decode(AskRequestedNormalized.self, forKey: key))
      case .askResolved:
        return .askResolved(try container.decode(AskResolvedNormalized.self, forKey: key))
      case .controlContext:
        return .controlContext(try container.decode(ControlContextNormalized.self, forKey: key))
      case .controlError:
        return .controlError(try container.decode(ControlErrorNormalized.self, forKey: key))
      case .controlInterrupt:
        return .controlInterrupt(try container.decode(ControlInterruptNormalized.self, forKey: key))
      case .controlModel:
        return .controlModel(try container.decode(ControlModelNormalized.self, forKey: key))
      case .controlNotification:
        return .controlNotification(try container.decode(ControlNotificationNormalized.self, forKey: key))
      case .controlPairingApproved:
        return .controlPairingApproved(try container.decode(ControlPairingApprovedNormalized.self, forKey: key))
      case .controlPairingDenied:
        return .controlPairingDenied(try container.decode(ControlPairingDeniedNormalized.self, forKey: key))
      case .controlPairingRequested:
        return .controlPairingRequested(try container.decode(ControlPairingRequestedNormalized.self, forKey: key))
      case .controlRateLimit:
        return .controlRateLimit(try container.decode(ControlRateLimitNormalized.self, forKey: key))
      case .controlRaw:
        return .controlRaw(try container.decode(ControlRawNormalized.self, forKey: key))
      case .controlStatus:
        return .controlStatus(try container.decode(ControlStatusNormalized.self, forKey: key))
      case .controlSteer:
        return .controlSteer(try container.decode(ControlSteerNormalized.self, forKey: key))
      case .controlTerminalAttached:
        return .controlTerminalAttached(try container.decode(ControlTerminalAttachedNormalized.self, forKey: key))
      case .controlTerminalClosed:
        return .controlTerminalClosed(try container.decode(ControlTerminalClosedNormalized.self, forKey: key))
      case .controlTerminalDetached:
        return .controlTerminalDetached(try container.decode(ControlTerminalDetachedNormalized.self, forKey: key))
      case .controlTerminalOpened:
        return .controlTerminalOpened(try container.decode(ControlTerminalOpenedNormalized.self, forKey: key))
      case .controlTrustGranted:
        return .controlTrustGranted(try container.decode(ControlTrustGrantedNormalized.self, forKey: key))
      case .controlTrustRevoked:
        return .controlTrustRevoked(try container.decode(ControlTrustRevokedNormalized.self, forKey: key))
      case .controlWarning:
        return .controlWarning(try container.decode(ControlWarningNormalized.self, forKey: key))
      case .hookCompleted:
        return .hookCompleted(try container.decode(HookCompletedNormalized.self, forKey: key))
      case .hookProgress:
        return .hookProgress(try container.decode(HookProgressNormalized.self, forKey: key))
      case .hookStarted:
        return .hookStarted(try container.decode(HookStartedNormalized.self, forKey: key))
      case .memoryCompacted:
        return .memoryCompacted(try container.decode(MemoryCompactedNormalized.self, forKey: key))
      case .memoryCompacting:
        return .memoryCompacting(try container.decode(MemoryCompactingNormalized.self, forKey: key))
      case .messageAssistant:
        return .messageAssistant(try container.decode(MessageAssistantNormalized.self, forKey: key))
      case .messageDelta:
        return .messageDelta(try container.decode(MessageDeltaNormalized.self, forKey: key))
      case .messageMedia:
        return .messageMedia(try container.decode(MessageMediaNormalized.self, forKey: key))
      case .messageStarted:
        return .messageStarted(try container.decode(MessageStartedNormalized.self, forKey: key))
      case .messageUser:
        return .messageUser(try container.decode(MessageUserNormalized.self, forKey: key))
      case .planDelta:
        return .planDelta(try container.decode(PlanDeltaNormalized.self, forKey: key))
      case .planUpdated:
        return .planUpdated(try container.decode(PlanUpdatedNormalized.self, forKey: key))
      case .queueCancelled:
        return .queueCancelled(try container.decode(QueueCancelledNormalized.self, forKey: key))
      case .queueDequeued:
        return .queueDequeued(try container.decode(QueueDequeuedNormalized.self, forKey: key))
      case .queueFailed:
        return .queueFailed(try container.decode(QueueFailedNormalized.self, forKey: key))
      case .queueQueued:
        return .queueQueued(try container.decode(QueueQueuedNormalized.self, forKey: key))
      case .queueSteered:
        return .queueSteered(try container.decode(QueueSteeredNormalized.self, forKey: key))
      case .reasoningCompleted:
        return .reasoningCompleted(try container.decode(ReasoningCompletedNormalized.self, forKey: key))
      case .reasoningDelta:
        return .reasoningDelta(try container.decode(ReasoningDeltaNormalized.self, forKey: key))
      case .reasoningStarted:
        return .reasoningStarted(try container.decode(ReasoningStartedNormalized.self, forKey: key))
      case .sessionDeleted:
        return .sessionDeleted(try container.decode(SessionDeletedNormalized.self, forKey: key))
      case .sessionNative:
        return .sessionNative(try container.decode(SessionNativeNormalized.self, forKey: key))
      case .sessionStarted:
        return .sessionStarted(try container.decode(SessionStartedNormalized.self, forKey: key))
      case .sessionUpdated:
        return .sessionUpdated(try container.decode(SessionUpdatedNormalized.self, forKey: key))
      case .toolCompleted:
        return .toolCompleted(try container.decode(ToolCompletedNormalized.self, forKey: key))
      case .toolDiff:
        return .toolDiff(try container.decode(ToolDiffNormalized.self, forKey: key))
      case .toolOutputDelta:
        return .toolOutputDelta(try container.decode(ToolOutputDeltaNormalized.self, forKey: key))
      case .toolProgress:
        return .toolProgress(try container.decode(ToolProgressNormalized.self, forKey: key))
      case .toolStarted:
        return .toolStarted(try container.decode(ToolStartedNormalized.self, forKey: key))
      case .toolTodo:
        return .toolTodo(try container.decode(ToolTodoNormalized.self, forKey: key))
      case .turnCancelled:
        return .turnCancelled(try container.decode(TurnCancelledNormalized.self, forKey: key))
      case .turnCompleted:
        return .turnCompleted(try container.decode(TurnCompletedNormalized.self, forKey: key))
      case .turnFailed:
        return .turnFailed(try container.decode(TurnFailedNormalized.self, forKey: key))
      case .turnReplaced:
        return .turnReplaced(try container.decode(TurnReplacedNormalized.self, forKey: key))
      case .turnStarted:
        return .turnStarted(try container.decode(TurnStartedNormalized.self, forKey: key))
      case .workspaceConnection:
        return .workspaceConnection(try container.decode(WorkspaceConnectionNormalized.self, forKey: key))
      case .workspaceCreated:
        return .workspaceCreated(try container.decode(WorkspaceCreatedNormalized.self, forKey: key))
      case .workspaceRemoved:
        return .workspaceRemoved(try container.decode(WorkspaceRemovedNormalized.self, forKey: key))
      }
    }

    init(from decoder: Decoder) throws {
      let value = try JSONValue(from: decoder)
      self = .controlRaw(ControlRawNormalized(["value": value]))
    }

    func encode(to encoder: Encoder) throws {
      switch self {
      case .approvalRequested(let value): try value.encode(to: encoder)
      case .approvalResolved(let value): try value.encode(to: encoder)
      case .approvalResponded(let value): try value.encode(to: encoder)
      case .askRequested(let value): try value.encode(to: encoder)
      case .askResolved(let value): try value.encode(to: encoder)
      case .controlContext(let value): try value.encode(to: encoder)
      case .controlError(let value): try value.encode(to: encoder)
      case .controlInterrupt(let value): try value.encode(to: encoder)
      case .controlModel(let value): try value.encode(to: encoder)
      case .controlNotification(let value): try value.encode(to: encoder)
      case .controlPairingApproved(let value): try value.encode(to: encoder)
      case .controlPairingDenied(let value): try value.encode(to: encoder)
      case .controlPairingRequested(let value): try value.encode(to: encoder)
      case .controlRateLimit(let value): try value.encode(to: encoder)
      case .controlRaw(let value): try value.encode(to: encoder)
      case .controlStatus(let value): try value.encode(to: encoder)
      case .controlSteer(let value): try value.encode(to: encoder)
      case .controlTerminalAttached(let value): try value.encode(to: encoder)
      case .controlTerminalClosed(let value): try value.encode(to: encoder)
      case .controlTerminalDetached(let value): try value.encode(to: encoder)
      case .controlTerminalOpened(let value): try value.encode(to: encoder)
      case .controlTrustGranted(let value): try value.encode(to: encoder)
      case .controlTrustRevoked(let value): try value.encode(to: encoder)
      case .controlWarning(let value): try value.encode(to: encoder)
      case .hookCompleted(let value): try value.encode(to: encoder)
      case .hookProgress(let value): try value.encode(to: encoder)
      case .hookStarted(let value): try value.encode(to: encoder)
      case .memoryCompacted(let value): try value.encode(to: encoder)
      case .memoryCompacting(let value): try value.encode(to: encoder)
      case .messageAssistant(let value): try value.encode(to: encoder)
      case .messageDelta(let value): try value.encode(to: encoder)
      case .messageMedia(let value): try value.encode(to: encoder)
      case .messageStarted(let value): try value.encode(to: encoder)
      case .messageUser(let value): try value.encode(to: encoder)
      case .planDelta(let value): try value.encode(to: encoder)
      case .planUpdated(let value): try value.encode(to: encoder)
      case .queueCancelled(let value): try value.encode(to: encoder)
      case .queueDequeued(let value): try value.encode(to: encoder)
      case .queueFailed(let value): try value.encode(to: encoder)
      case .queueQueued(let value): try value.encode(to: encoder)
      case .queueSteered(let value): try value.encode(to: encoder)
      case .reasoningCompleted(let value): try value.encode(to: encoder)
      case .reasoningDelta(let value): try value.encode(to: encoder)
      case .reasoningStarted(let value): try value.encode(to: encoder)
      case .sessionDeleted(let value): try value.encode(to: encoder)
      case .sessionNative(let value): try value.encode(to: encoder)
      case .sessionStarted(let value): try value.encode(to: encoder)
      case .sessionUpdated(let value): try value.encode(to: encoder)
      case .toolCompleted(let value): try value.encode(to: encoder)
      case .toolDiff(let value): try value.encode(to: encoder)
      case .toolOutputDelta(let value): try value.encode(to: encoder)
      case .toolProgress(let value): try value.encode(to: encoder)
      case .toolStarted(let value): try value.encode(to: encoder)
      case .toolTodo(let value): try value.encode(to: encoder)
      case .turnCancelled(let value): try value.encode(to: encoder)
      case .turnCompleted(let value): try value.encode(to: encoder)
      case .turnFailed(let value): try value.encode(to: encoder)
      case .turnReplaced(let value): try value.encode(to: encoder)
      case .turnStarted(let value): try value.encode(to: encoder)
      case .workspaceConnection(let value): try value.encode(to: encoder)
      case .workspaceCreated(let value): try value.encode(to: encoder)
      case .workspaceRemoved(let value): try value.encode(to: encoder)
      }
    }
  }

}
